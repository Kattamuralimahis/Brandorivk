diff --git a/index.html b/index.html
index 3f6123daf3f016cb879b44f7788f7aebaca49e87..59411a418ac51e87041b3c1d7975a26b965b8fa1 100644
--- a/index.html
+++ b/index.html
@@ -1,276 +1,289 @@
-<!DOCTYPE html>
-<html lang="en">
-<head>
-    <meta charset="UTF-8">
-    <meta name="viewport" content="width=device-width, initial-scale=1.0">
-    <title>Brandorivk | Premium Apparel</title>
-    <style>
-        * { margin: 0; padding: 0; box-sizing: border-box; }
-        body { font-family: 'Segoe UI', Arial, sans-serif; background: #f4f4f4; color: #1a1a1a; }
-        header { background: white; padding: 1rem 5%; display: flex; justify-content: space-between; align-items: center; box-shadow: 0 2px 5px rgba(0,0,0,0.1); position: sticky; top: 0; z-index: 100; }
-        .logo { font-size: 1.5rem; font-weight: 800; text-transform: uppercase; cursor: pointer; }
-        .logo span { color: #e67e22; }
-        .cart-icon { cursor: pointer; padding: 8px 16px; background: #f4f4f4; border-radius: 6px; font-weight: 600; }
-        .cart-icon:hover { background: #e67e22; color: white; }
-        .cart-count { background: #e67e22; color: white; border-radius: 50%; padding: 2px 8px; margin-left: 5px; }
-        .hero { background: linear-gradient(135deg, #1a1a1a 0%, #2c2c2c 100%); color: white; text-align: center; padding: 4rem 1rem; }
-        .hero h1 { font-size: 2.5rem; margin-bottom: 1rem; }
-        .hero p { font-size: 1.1rem; margin-bottom: 1.5rem; }
-        .hero button { padding: 12px 30px; background: #e67e22; color: white; border: none; border-radius: 6px; font-size: 1rem; font-weight: 600; cursor: pointer; }
-        .hero button:hover { background: #d35400; }
-        .filters { text-align: center; padding: 2rem 1rem; }
-        .filter-btn { padding: 10px 25px; margin: 5px; border: 2px solid #1a1a1a; background: white; border-radius: 25px; cursor: pointer; font-size: 1rem; font-weight: 500; }
-        .filter-btn:hover, .filter-btn.active { background: #1a1a1a; color: white; }
-        .container { max-width: 1200px; margin: 0 auto; padding: 0 1rem 3rem; }
-        .product-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(260px, 1fr)); gap: 1.5rem; }
-        .product-card { background: white; border-radius: 12px; overflow: hidden; box-shadow: 0 4px 15px rgba(0,0,0,0.08); transition: transform 0.2s; }
-        .product-card:hover { transform: translateY(-5px); }
-        .product-image { height: 220px; background: linear-gradient(135deg, #f5f5f5 0%, #e0e0e0 100%); display: flex; align-items: center; justify-content: center; font-size: 4rem; overflow: hidden; position: relative; }
-        .product-image img { width: 100%; height: 100%; object-fit: cover; display: block; }
-        .product-image .placeholder { position: absolute; font-size: 3rem; color: #999; }
-        .product-info { padding: 1.3rem; }
-        .product-category { font-size: 0.75rem; color: #e67e22; text-transform: uppercase; font-weight: 600; }
-        .product-title { font-size: 1.15rem; margin: 0.4rem 0; font-weight: 600; }
-        .product-price { font-weight: 700; font-size: 1.3rem; color: #1a1a1a; }
-        .add-btn { width: 100%; padding: 12px; margin-top: 1rem; background: #1a1a1a; color: white; border: none; border-radius: 6px; font-size: 1rem; font-weight: 600; cursor: pointer; }
-        .add-btn:hover { background: #e67e22; }
-        .cart-modal { display: none; position: fixed; top: 0; right: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.6); z-index: 200; justify-content: flex-end; }
-        .cart-content { background: white; width: 400px; max-width: 90%; height: 100%; padding: 1.5rem; display: flex; flex-direction: column; overflow-y: auto; }
-        .cart-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 1.5rem; padding-bottom: 1rem; border-bottom: 1px solid #eee; }
-        .close-cart { cursor: pointer; font-size: 2rem; line-height: 1; }
-        .cart-item { display: flex; justify-content: space-between; align-items: center; padding: 1rem 0; border-bottom: 1px solid #f4f4f4; }
-        .cart-total { margin-top: auto; padding-top: 1rem; border-top: 2px solid #1a1a1a; font-size: 1.3rem; font-weight: 700; }
-        .checkout-btn { width: 100%; padding: 15px; margin-top: 1rem; background: #e67e22; color: white; border: none; border-radius: 8px; font-size: 1.1rem; font-weight: 600; cursor: pointer; }
-        .checkout-btn:hover { background: #d35400; }
-        .toast { position: fixed; bottom: 25px; right: 25px; background: #27ae60; color: white; padding: 14px 24px; border-radius: 10px; box-shadow: 0 6px 20px rgba(0,0,0,0.2); z-index: 1000; display: none; animation: slideIn 0.3s ease; }
-        .toast.error { background: #e74c3c; }
-        @keyframes slideIn { from { transform: translateX(100%); opacity: 0; } to { transform: translateX(0); opacity: 1; } }
-        footer { background: #1a1a1a; color: white; text-align: center; padding: 2rem; }
-        @media (max-width: 600px) { .cart-content { width: 100%; } .hero h1 { font-size: 2rem; } .product-grid { grid-template-columns: repeat(auto-fill, minmax(200px, 1fr)); } }
-    </style>
-</head>
-<body>
-    <header>
-        <div class="logo" onclick="window.scrollTo({top: 0, behavior: 'smooth'})">Brand<span>orivk</span></div>
-        <div class="cart-icon" onclick="toggleCart()">🛒 Cart <span class="cart-count" id="cartCount">0</span></div>
-    </header>
-    
-    <section class="hero">
-        <h1>Define Your Style</h1>
-        <p>Exclusive collection of T-shirts, Shirts, and Pants.</p>
-        <button onclick="document.getElementById('products').scrollIntoView({behavior: 'smooth'})">Shop Now</button>
-    </section>
-    
-    <div class="filters" id="products">
-        <button class="filter-btn active" onclick="filterProducts('all')">All</button>
-        <button class="filter-btn" onclick="filterProducts('tshirt')">T-Shirts</button>
-        <button class="filter-btn" onclick="filterProducts('shirt')">Shirts</button>
-        <button class="filter-btn" onclick="filterProducts('pants')">Pants</button>
-    </div>
-    
-    <div class="container">
-        <div class="product-grid" id="productGrid"></div>
-    </div>
-    
-    <footer><p>© 2024 Brandorivk. All rights reserved.</p></footer>
-    
-    <div class="cart-modal" id="cartModal">
-        <div class="cart-content">
-            <div class="cart-header">
-                <h2>Your Cart</h2>
-                <span class="close-cart" onclick="toggleCart()">&times;</span>
-            </div>
-            <div id="cartItems"><p style="text-align:center; padding:2rem; color:#888;">Your cart is empty 🛒</p></div>
-            <div class="cart-total">Total: $<span id="cartTotal">0.00</span></div>
-            <button class="checkout-btn" onclick="checkout()">Checkout</button>
-        </div>
-    </div>
-    
-    <div class="toast" id="toast"></div>
-
-    <script>
-        // ===== PRODUCTS - USE GITHUB RAW URLs OR IMGUR =====
-        const products = [
-            { 
-                id: 1, 
-                name: "Classic White Tee", 
-                category: "tshirt", 
-                price: 25.00, 
-                // ✅ Use GitHub Raw URL (upload image to your repo first):
-                image: "https://raw.githubusercontent.com/Kattamuralimahis/Brandorivk/main/white-tee.jpg",
-                // ❌ WordPress URLs often block external sites:
-                // image: "https://murali56.wordpress.com/wp-content/uploads/2026/02/whatsapp-image-2026-02-18-at-19.11.49.jpeg",
-                description: "100% premium cotton, breathable fit", 
-                stock: 15 
-            },
-            { id: 2, name: "Black Graphic Tee", category: "tshirt", price: 30.00, image: "https://via.placeholder.com/500x500/1a1a1a/ffffff?text=Black+Tee", description: "Premium print, fade-resistant", stock: 8 },
-            { id: 3, name: "Oxford Blue Shirt", category: "shirt", price: 45.00, image: "https://via.placeholder.com/500x500/667eea/ffffff?text=Blue+Shirt", description: "Wrinkle-free, office ready", stock: 12 },
-            { id: 4, name: "Flannel Check Shirt", category: "shirt", price: 50.00, image: "https://via.placeholder.com/500x500/e67e22/ffffff?text=Flannel", description: "Warm brushed cotton", stock: 6 },
-            { id: 5, name: "Slim Fit Chinos", category: "pants", price: 55.00, image: "https://via.placeholder.com/500x500/27ae60/ffffff?text=Chinos", description: "Stretch fabric, all-day comfort", stock: 20 },
-            { id: 6, name: "Denim Jeans", category: "pants", price: 60.00, image: "https://via.placeholder.com/500x500/3498db/ffffff?text=Jeans", description: "Classic cut, durable denim", stock: 10 },
-            { id: 7, name: "Vintage Band Tee", category: "tshirt", price: 35.00, image: "https://via.placeholder.com/500x500/9b59b6/ffffff?text=Vintage", description: "Retro design, soft wash", stock: 5 },
-            { id: 8, name: "Linen Summer Shirt", category: "shirt", price: 40.00, image: "https://via.placeholder.com/500x500/1abc9c/ffffff?text=Linen", description: "Lightweight, perfect for summer", stock: 9 }
-        ];
-
-        let cart = JSON.parse(localStorage.getItem('brandorivk_cart')) || [];
-
-        function renderProducts(filter = 'all') {
-            const grid = document.getElementById('productGrid');
-            grid.innerHTML = '';
-            
-            const filtered = filter === 'all' ? products : products.filter(p => p.category === filter);
-            
-            if (filtered.length === 0) {
-                grid.innerHTML = '<p style="grid-column: 1/-1; text-align: center; padding: 2rem; color: #666;">No products found.</p>';
-                return;
-            }
-            
-            filtered.forEach(p => {
-                const inCart = cart.find(item => item.id === p.id);
-                const card = document.createElement('div');
-                card.className = 'product-card';
-                card.innerHTML = `
-                    <div class="product-image">
-                        <span class="placeholder" id="ph-${p.id}">👕</span>
-                        <img src="${p.image}" alt="${p.name}" 
-                             crossorigin="anonymous"
-                             referrerpolicy="no-referrer"
-                             onload="document.getElementById('ph-${p.id}').style.display='none'"
-                             onerror="this.style.display='none'; document.getElementById('ph-${p.id}').style.display='block'">
-                    </div>
-                    <div class="product-info">
-                        <div class="product-category">${p.category}</div>
-                        <h3 class="product-title">${p.name}</h3>
-                        <p style="font-size: 0.9rem; color: #666; margin: 0.3rem 0;">${p.description}</p>
-                        <div class="product-price">$${p.price.toFixed(2)}</div>
-                        <div style="font-size: 0.8rem; color: ${p.stock < 5 ? '#e74c3c' : '#27ae60'}; margin: 0.3rem 0; font-weight: 500;">
-                            ${p.stock < 5 ? '🔥 Only ' + p.stock + ' left!' : '✅ In Stock'}
-                        </div>
-                        <button class="add-btn" onclick="addToCart(${p.id})" ${p.stock <= 0 ? 'disabled style="opacity:0.6; cursor:not-allowed;"' : ''}>
-                            ${inCart ? '✓ In Cart' : 'Add to Cart'}
-                        </button>
-                    </div>
-                `;
-                grid.appendChild(card);
-            });
-            
-            console.log('✅ Rendered', filtered.length, 'products');
-        }
-
-        function filterProducts(category) {
-            document.querySelectorAll('.filter-btn').forEach(btn => btn.classList.remove('active'));
-            event.target.classList.add('active');
-            renderProducts(category);
-        }
-
-        function addToCart(id) {
-            const product = products.find(p => p.id === id);
-            if (!product || product.stock <= 0) {
-                showToast('Sorry, this item is out of stock!', 'error');
-                return;
-            }
-            const existing = cart.find(item => item.id === id);
-            if (existing) {
-                if (existing.quantity < product.stock) {
-                    existing.quantity++;
-                    showToast('+1 ' + product.name + ' added');
-                } else {
-                    showToast('Maximum quantity reached', 'error');
-                    return;
-                }
-            } else {
-                cart.push({ ...product, quantity: 1 });
-                showToast(product.name + ' added to cart!');
-            }
-            localStorage.setItem('brandorivk_cart', JSON.stringify(cart));
-            updateCartUI();
-        }
-
-        function removeFromCart(index) {
-            cart.splice(index, 1);
-            localStorage.setItem('brandorivk_cart', JSON.stringify(cart));
-            updateCartUI();
-            showToast('Item removed');
-        }
-
-        function updateQuantity(index, change) {
-            const item = cart[index];
-            const product = products.find(p => p.id === item.id);
-            if (item.quantity + change <= 0) { removeFromCart(index); return; }
-            if (item.quantity + change > product.stock) { showToast('Max stock reached', 'error'); return; }
-            item.quantity += change;
-            localStorage.setItem('brandorivk_cart', JSON.stringify(cart));
-            updateCartUI();
-        }
-
-        function updateCartUI() {
-            const cartCount = document.getElementById('cartCount');
-            const cartItems = document.getElementById('cartItems');
-            const cartTotal = document.getElementById('cartTotal');
-            
-            const totalItems = cart.reduce((sum, item) => sum + item.quantity, 0);
-            cartCount.textContent = totalItems;
-            
-            if (cart.length === 0) {
-                cartItems.innerHTML = '<p style="text-align:center; padding:2rem; color:#888;">Your cart is empty 🛒</p>';
-                cartTotal.textContent = '0.00';
-                return;
-            }
-            
-            let total = 0;
-            cartItems.innerHTML = '';
-            
-            cart.forEach((item, index) => {
-                const itemTotal = item.price * item.quantity;
-                total += itemTotal;
-                const itemEl = document.createElement('div');
-                itemEl.className = 'cart-item';
-                itemEl.innerHTML = `
-                    <div><strong>${item.name}</strong><br><small style="color:#888;">$${item.price.toFixed(2)} × ${item.quantity}</small></div>
-                    <div style="display:flex; align-items:center; gap:8px;">
-                        <button onclick="updateQuantity(${index}, -1)" style="width:28px; height:28px; border:1px solid #ddd; background:white; cursor:pointer; border-radius:4px;">-</button>
-                        <span>${item.quantity}</span>
-                        <button onclick="updateQuantity(${index}, 1)" style="width:28px; height:28px; border:1px solid #ddd; background:white; cursor:pointer; border-radius:4px;">+</button>
-                        <strong style="margin-left:12px;">$${itemTotal.toFixed(2)}</strong>
-                        <button onclick="removeFromCart(${index})" style="background:none; border:none; color:#e74c3c; cursor:pointer; font-size:1.2rem;">×</button>
-                    </div>
-                `;
-                cartItems.appendChild(itemEl);
-            });
-            cartTotal.textContent = total.toFixed(2);
-        }
-
-        function checkout() {
-            if (cart.length === 0) { showToast('Cart is empty!', 'error'); return; }
-            alert('🎉 Order placed! (Demo mode)');
-            cart = [];
-            localStorage.removeItem('brandorivk_cart');
-            updateCartUI();
-        }
-
-        function toggleCart() {
-            const modal = document.getElementById('cartModal');
-            modal.style.display = modal.style.display === 'flex' ? 'none' : 'flex';
-            if (modal.style.display === 'flex') updateCartUI();
-        }
-
-        document.getElementById('cartModal')?.addEventListener('click', (e) => {
-            if (e.target === document.getElementById('cartModal')) toggleCart();
-        });
-
-        function showToast(message, type = 'success') {
-            const toast = document.getElementById('toast');
-            toast.textContent = message;
-            toast.className = 'toast' + (type === 'error' ? ' error' : '');
-            toast.style.display = 'block';
-            setTimeout(() => toast.style.display = 'none', 2500);
-        }
-
-        document.addEventListener('DOMContentLoaded', () => {
-            console.log('✅ Page loaded - Products:', products.length);
-            renderProducts();
-            updateCartUI();
-        });
-    </script>
-</body>
-</html>
\ No newline at end of file
+<!DOCTYPE html>
+<html lang="en">
+<head>
+  <meta charset="UTF-8" />
+  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
+  <title>Brandorivk | Smooth Ecommerce</title>
+  <style>
+    :root {
+      --bg: #f5f7fb;
+      --text: #20253a;
+      --muted: #6a728f;
+      --primary: #6956ff;
+      --card: #fff;
+      --line: #e9edfb;
+      --radius: 16px;
+    }
+    * { box-sizing: border-box; margin: 0; padding: 0; }
+    body {
+      font-family: Inter, "Segoe UI", Arial, sans-serif;
+      color: var(--text);
+      background: radial-gradient(circle at top, #fdfdff 0%, var(--bg) 60%);
+    }
+    header {
+      position: sticky; top: 0; z-index: 20;
+      backdrop-filter: blur(8px);
+      background: rgba(255,255,255,.88);
+      border-bottom: 1px solid var(--line);
+      display: flex; justify-content: space-between; align-items: center;
+      padding: 12px 5%;
+    }
+    .logo { display: inline-flex; align-items: center; gap: 10px; font-size: 1.05rem; font-weight: 700; min-width: 0; }
+    .logo img { width: 120px; max-width: 32vw; height: auto; display: block; object-fit: contain; }
+    .logo span { white-space: nowrap; color: #2a2f45; }
+    .cart-pill {
+      border: 1px solid var(--line); background: #fff;
+      padding: 10px 14px; border-radius: 999px;
+      cursor: pointer; font-weight: 600;
+      box-shadow: 0 5px 16px rgba(22, 40, 88, .08);
+    }
+    .hero {
+      max-width: 1120px; margin: 26px auto 20px; padding: 44px 5%;
+      border-radius: calc(var(--radius) + 6px);
+      background: linear-gradient(135deg, #171f45, #2a3067 55%, #403ab4);
+      color: #fff;
+      box-shadow: 0 20px 40px rgba(40, 53, 108, .26);
+    }
+    .hero h1 { font-size: clamp(2rem, 4vw, 3rem); margin-bottom: 12px; }
+    .hero p { max-width: 550px; color: #dbe2ff; line-height: 1.6; }
+    .hero button {
+      margin-top: 24px; padding: 12px 22px;
+      border: 0; border-radius: 10px;
+      background: #fff; color: #1f2158;
+      font-weight: 700; cursor: pointer;
+      transition: transform .25s ease;
+    }
+    .hero button:hover { transform: translateY(-2px); }
+    .wrap { max-width: 1120px; margin: 24px auto 65px; padding: 0 5%; }
+    .filters { display: flex; gap: 10px; flex-wrap: wrap; margin-bottom: 18px; }
+    .filter {
+      border: 1px solid var(--line); background: #fff; color: var(--muted);
+      border-radius: 999px; padding: 9px 14px; cursor: pointer;
+      font-weight: 600;
+    }
+    .filter.active { background: var(--primary); color: #fff; border-color: var(--primary); }
+    .grid { display: grid; gap: 18px; grid-template-columns: repeat(auto-fill, minmax(240px, 1fr)); }
+    .card {
+      background: var(--card); border: 1px solid var(--line);
+      border-radius: var(--radius); overflow: hidden;
+      box-shadow: 0 10px 25px rgba(33, 51, 103, .08);
+      transition: transform .25s ease, box-shadow .25s ease;
+    }
+    .card:hover { transform: translateY(-5px); box-shadow: 0 16px 34px rgba(33, 51, 103, .12); }
+    .media { position: relative; aspect-ratio: 1 / 1; background: #f1f4ff; }
+    .media img { width: 100%; height: 100%; object-fit: cover; display: block; }
+    .price-tag {
+      position: absolute; top: 10px; left: 10px;
+      background: rgba(32,37,58,.84); color: #fff;
+      font-size: .74rem; padding: 5px 9px; border-radius: 999px;
+    }
+    .info { padding: 14px; }
+    .category { text-transform: uppercase; font-size: .7rem; letter-spacing: .8px; color: var(--primary); font-weight: 700; }
+    .title { margin-top: 6px; font-size: 1.02rem; font-weight: 700; }
+    .desc { margin-top: 8px; color: var(--muted); font-size: .9rem; min-height: 40px; line-height: 1.5; }
+    .price-row { margin-top: 10px; display: flex; justify-content: space-between; align-items: center; }
+    .price { font-size: 1.2rem; font-weight: 800; }
+    .stock { font-size: .78rem; color: #2ea35e; font-weight: 600; }
+    .add {
+      margin-top: 12px; width: 100%; padding: 10px;
+      border: 0; border-radius: 10px;
+      background: var(--primary); color: #fff;
+      font-weight: 700; cursor: pointer;
+    }
+    .cart {
+      position: fixed; inset: 0; display: none; justify-content: flex-end;
+      background: rgba(20,22,35,.56); z-index: 30;
+    }
+    .cart.open { display: flex; }
+    .cart-panel {
+      width: min(390px, 100%); background: #fff; height: 100%;
+      padding: 18px; display: flex; flex-direction: column;
+    }
+    .items { overflow: auto; margin-top: 12px; }
+    .item { display: flex; justify-content: space-between; border-bottom: 1px solid var(--line); padding: 10px 0; gap: 8px; }
+    .checkout { margin-top: auto; border-top: 2px solid var(--line); padding-top: 12px; }
+    .checkout button { margin-top: 10px; width: 100%; border: 0; border-radius: 10px; background: #1f2350; color: #fff; padding: 12px; cursor: pointer; }
+    .toast { position: fixed; right: 20px; bottom: 20px; background: #1e234f; color: #fff; padding: 10px 14px; border-radius: 10px; font-size: .9rem; box-shadow: 0 8px 24px rgba(0,0,0,.18); opacity: 0; transform: translateY(10px); pointer-events: none; transition: all .24s ease; z-index: 40; }
+    .toast.show { opacity: 1; transform: translateY(0); }
+    @media (max-width: 760px) {
+      .logo { gap: 6px; }
+      .logo img { width: 100px; max-width: 38vw; }
+      .logo span { display: none; }
+      .cart-pill { padding: 8px 11px; font-size: .92rem; }
+      .hero { margin-top: 16px; }
+    }
+  </style>
+</head>
+<body>
+  <header>
+    <div class="logo">
+      <img src="assets/logo/brandorivk-logo.svg" alt="Brandorivk logo" />
+      <span>Premium streetwear essentials</span>
+    </div>
+    <button class="cart-pill" id="cartButton">🛒 Cart (<span id="cartCount">0</span>)</button>
+  </header>
+
+  <section class="hero">
+    <h1>Brandorivk: Built for Bold Everyday Style.</h1>
+    <p>Brandorivk delivers clean, confident fashion with quality fabrics and modern fits. This storefront is focused on smooth browsing, clear product details, and reliable WordPress image rendering for your uploaded catalog.</p>
+    <button onclick="document.getElementById('shop').scrollIntoView({ behavior: 'smooth' })">Browse Collection</button>
+  </section>
+
+  <main class="wrap" id="shop">
+    <div class="filters" id="filters"></div>
+    <section class="grid" id="grid"></section>
+  </main>
+
+  <aside class="cart" id="cartModal" aria-hidden="true">
+    <div class="cart-panel">
+      <div style="display:flex; justify-content:space-between; align-items:center;">
+        <h3>Your Cart</h3>
+        <button id="closeCart" style="border:0; background:none; font-size:1.8rem; cursor:pointer;">×</button>
+      </div>
+      <div class="items" id="cartItems"></div>
+      <div class="checkout">
+        <div style="display:flex; justify-content:space-between; font-weight:700; font-size:1.05rem;">
+          <span>Total</span><span>$<span id="total">0.00</span></span>
+        </div>
+        <button onclick="location.href='checkout.html'">Continue to Checkout</button>
+      </div>
+    </div>
+  </aside>
+
+  <div class="toast" id="toast"></div>
+
+  <script type="module">
+    import { products } from './data/products.js';
+
+    const filters = [
+      { key: 'all', label: 'All Products' },
+      { key: 'tshirt', label: 'T-Shirts' },
+      { key: 'shirt', label: 'Shirts' },
+      { key: 'pants', label: 'Pants' }
+    ];
+
+    const cartKey = 'brandorivk_cart';
+    let activeFilter = 'all';
+    let cart = JSON.parse(localStorage.getItem(cartKey) || '[]');
+
+    const grid = document.getElementById('grid');
+    const filterWrap = document.getElementById('filters');
+    const cartModal = document.getElementById('cartModal');
+    const toast = document.getElementById('toast');
+
+    function normalizeImageUrl(url) {
+      if (!url) return '';
+      const isWordpress = /\/wp-content\//i.test(url) || /wordpress/i.test(url);
+      return isWordpress ? `${url}${url.includes('?') ? '&' : '?'}quality=85` : url;
+    }
+
+    function renderFilters() {
+      filterWrap.innerHTML = filters.map(({ key, label }) =>
+        `<button class="filter ${activeFilter === key ? 'active' : ''}" data-key="${key}">${label}</button>`
+      ).join('');
+
+      filterWrap.querySelectorAll('button').forEach(btn => {
+        btn.addEventListener('click', () => {
+          activeFilter = btn.dataset.key;
+          renderFilters();
+          renderProducts();
+        });
+      });
+    }
+
+    function renderProducts() {
+      const visible = activeFilter === 'all'
+        ? products
+        : products.filter(product => product.category === activeFilter);
+
+      grid.innerHTML = visible.map(product => `
+        <article class="card">
+          <div class="media">
+            <span class="price-tag">${product.priceTag}</span>
+            <img src="${normalizeImageUrl(product.image)}"
+                 alt="${product.name}"
+                 loading="lazy"
+                 referrerpolicy="no-referrer-when-downgrade"
+                 onerror="this.onerror=null; this.src='${product.placeholder}'">
+          </div>
+          <div class="info">
+            <div class="category">${product.category}</div>
+            <h3 class="title">${product.name}</h3>
+            <p class="desc">${product.description}</p>
+            <div class="price-row">
+              <p class="price">$${product.price.toFixed(2)}</p>
+              <p class="stock">${product.stock > 0 ? `${product.stock} in stock` : 'out of stock'}</p>
+            </div>
+            <button class="add" data-id="${product.id}" ${product.stock <= 0 ? 'disabled' : ''}>${(cart.find(item => item.id === product.id)?.quantity || 0) > 0 ? `Add More (${cart.find(item => item.id === product.id).quantity})` : 'Add to Cart'}</button>
+          </div>
+        </article>
+      `).join('');
+
+      grid.querySelectorAll('.add').forEach(button => {
+        button.addEventListener('click', () => addToCart(Number(button.dataset.id)));
+      });
+    }
+
+
+    function showToast(message) {
+      toast.textContent = message;
+      toast.classList.add('show');
+      clearTimeout(showToast.timeout);
+      showToast.timeout = setTimeout(() => toast.classList.remove('show'), 1400);
+    }
+
+    function addToCart(id) {
+      const product = products.find(item => item.id === id);
+      if (!product) return;
+      const existing = cart.find(item => item.id === id);
+      if (existing) existing.quantity += 1;
+      else cart.push({ ...product, quantity: 1 });
+      localStorage.setItem(cartKey, JSON.stringify(cart));
+      renderCart();
+      renderProducts();
+      showToast(`${product.name} added to cart`);
+    }
+
+    function renderCart() {
+      const totalItems = cart.reduce((sum, item) => sum + item.quantity, 0);
+      const totalPrice = cart.reduce((sum, item) => sum + (item.price * item.quantity), 0);
+      document.getElementById('cartCount').textContent = totalItems;
+      document.getElementById('total').textContent = totalPrice.toFixed(2);
+
+      const items = document.getElementById('cartItems');
+      if (!cart.length) {
+        items.innerHTML = '<p style="color:#76809b; padding:18px 0;">Cart is empty.</p>';
+        return;
+      }
+
+      items.innerHTML = cart.map((item, idx) => `
+        <div class="item">
+          <div>
+            <strong>${item.name}</strong>
+            <p style="font-size:.84rem; color:#6a728f;">$${item.price.toFixed(2)} × ${item.quantity}</p>
+          </div>
+          <button data-remove="${idx}" style="border:0;background:#ffecef;color:#c02743;border-radius:7px;padding:6px 10px;cursor:pointer;">Remove</button>
+        </div>
+      `).join('');
+
+      items.querySelectorAll('[data-remove]').forEach(button => {
+        button.addEventListener('click', () => {
+          cart.splice(Number(button.dataset.remove), 1);
+          localStorage.setItem(cartKey, JSON.stringify(cart));
+          renderCart();
+        });
+      });
+    }
+
+    document.getElementById('cartButton').addEventListener('click', () => cartModal.classList.add('open'));
+    document.getElementById('closeCart').addEventListener('click', () => cartModal.classList.remove('open'));
+    cartModal.addEventListener('click', (event) => {
+      if (event.target === cartModal) cartModal.classList.remove('open');
+    });
+
+    renderFilters();
+    renderProducts();
+    renderCart();
+  </script>
+</body>
+</html>
